name: PRoot Node/npm Regression

on:
  push:
    branches:
      - fix/proot-node-npm-regression
      - feature/interactive-session
      - main
      - master
    paths:
      - 'app/src/main/assets/proot/**'
      - 'app/src/main/assets/rootfs/**'
      - 'app/src/main/java/me/rerere/rikkahub/data/container/**'
      - 'app/src/androidTest/java/me/rerere/rikkahub/data/container/PRootNodeNpmRegressionTest.kt'
      - 'app/build.gradle.kts'
      - '.github/workflows/proot-node-npm-regression.yml'
  pull_request:
    paths:
      - 'app/src/main/assets/proot/**'
      - 'app/src/main/assets/rootfs/**'
      - 'app/src/main/java/me/rerere/rikkahub/data/container/**'
      - 'app/src/androidTest/java/me/rerere/rikkahub/data/container/PRootNodeNpmRegressionTest.kt'
      - 'app/build.gradle.kts'
      - '.github/workflows/proot-node-npm-regression.yml'
  workflow_dispatch:

jobs:
  node-npm-regression:
    name: Real npm workflow inside Android PRoot
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Node.js for web-ui build
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Create dummy google-services.json
        run: |
          cat > app/google-services.json << 'EOFGS'
          {
            "project_info": {
              "project_number": "000000000000",
              "project_id": "dummy-project",
              "storage_bucket": "dummy-project.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:000000000000:android:0000000000000000",
                  "android_client_info": {
                    "package_name": "me.rerere.rikkahub"
                  }
                },
                "api_key": [{"current_key": "AIzaSyDummyKey000000000000000000000000"}]
              },
              {
                "client_info": {
                  "mobilesdk_app_id": "1:000000000000:android:0000000000000001",
                  "android_client_info": {
                    "package_name": "me.rerere.rikkahub.dev.next"
                  }
                },
                "api_key": [{"current_key": "AIzaSyDummyKey000000000000000000000001"}]
              }
            ],
            "configuration_version": "1"
          }
          EOFGS

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Android PRoot Node/npm regression test
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          target: default
          profile: Nexus 6
          ram-size: 3072M
          heap-size: 768M
          disable-animations: true
          emulator-boot-timeout: 900
          emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
          script: |
            set -eux
            ./gradlew :app:assembleDebug :app:assembleDebugAndroidTest -PincludeX86_64AbiForTests=true --stacktrace
            adb wait-for-device
            adb devices
            adb shell getprop sys.boot_completed
            adb shell df -h || true
            adb shell pm trim-caches 999G || true
            adb install -r -d -g -t --no-incremental app/build/outputs/apk/debug/app-debug.apk
            adb install -r -d -g -t --no-incremental app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk
            adb shell am instrument -w -r               -e class me.rerere.rikkahub.data.container.PRootNodeNpmRegressionTest               me.rerere.rikkahub.dev.next.debug.test/androidx.test.runner.AndroidJUnitRunner

      - name: Upload instrumentation reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: proot-node-npm-instrumentation-reports
          path: |
            app/build/reports/androidTests/**
            app/build/outputs/androidTest-results/**
          retention-days: 7
