# EPUB 解析重构与专家审查备忘录

## 审查结论（世界级代码质量视角）

### 1) 架构问题（已修复）
- 原实现把 EPUB 的 XML 解析、HTML 清洗、封面识别、路径规范化、OCR 逻辑全部堆在 `DocumentTextExtractor.kt`。
- 导致：职责过重、可测性差、回归风险高。
- **修复**：拆分为独立 parser 子模块：
  - `data/document/epub/EpubTextParser.kt`
  - `data/document/epub/EpubModels.kt`
  - `data/document/epub/EpubPathResolver.kt`
- `DocumentTextExtractor` 现在只保留"统一路由 + 通用文本处理"，职责清晰。

### 2) 正确性边界（已修复）
- 封面识别兼容多来源：
  - `metadata/meta[name=cover]`
  - manifest `properties=cover-image`
  - OPF2 `guide/reference[type=cover]`
- 路径处理支持：
  - `../`, `./`
  - `#fragment`, `?query`
  - 反斜杠路径
  - 绝对路径与相对路径
- 命名空间鲁棒：
  - 通过 `tagName().substringAfter(':')` 做 localName 匹配，降低前缀差异影响。

### 3) 内容质量（已修复）
- Spine 章节提取时跳过 `properties=nav` 的目录资源，降低 TOC 噪音。
- HTML 清洗移除 script/style/nav/footnote 等非正文内容。
- 正文优先抽取语义块标签，失败再回退 `doc.text()`。

### 4) OCR 控制与性能（已修复）
- 会话 EPUB：明确传 `enableImageOcr = false`，不做 OCR。
- 知识库 EPUB：根据助手开关决定 OCR。
- OCR 前先做图片有效性门槛（宽高与面积），减少图标噪声和无效调用。

---

## 变更文件清单

### 新增
1. `app/src/main/java/me/rerere/rikkahub/data/document/epub/EpubModels.kt`
2. `app/src/main/java/me/rerere/rikkahub/data/document/epub/EpubPathResolver.kt`
3. `app/src/main/java/me/rerere/rikkahub/data/document/epub/EpubTextParser.kt`

### 重写
4. `app/src/main/java/me/rerere/rikkahub/data/document/DocumentTextExtractor.kt`

### 已对接（保持）
5. `app/src/main/java/me/rerere/rikkahub/data/ai/transformers/DocumentAsPromptTransformer.kt`
   - 会话 EPUB 禁用 OCR
6. `app/src/main/java/me/rerere/rikkahub/service/KnowledgeBaseService.kt`
   - 知识库 EPUB OCR 受开关控制

---

## 行为验收标准

### 会话上传 EPUB
- 上传成功
- 可提取正文
- 不出现图片 OCR 内容

### 知识库上传 EPUB（开关关闭）
- 上传与索引成功
- 仅正文可检索

### 知识库上传 EPUB（开关开启）
- 上传与索引成功
- 图片文字可检索
- 封面图默认不参与 OCR

---

## 回归测试建议

1. OPF3（含 nav + cover-image）
2. OPF2（含 guide cover）
3. href 含 fragment/query
4. Windows 风格路径反斜杠
5. 大量装饰小图（验证 OCR 门槛）

---

## 风险提示

- 本容器无 Java，无法执行 Gradle 编译验证。
- 建议在本地运行：
  - `./gradlew :app:compileDebugKotlin`
  - 重点观察 `DocumentTextExtractor` 与 `epub` 子包的编译与 lint。

---

# pip 超时设置不生效修复备忘录

## 问题现象

用户在高级设置中自定义了 pip install 超时时间（如 10s），但实际执行 pip install 命令时该设置不生效，始终使用容器命令的默认超时（300s）。

LLM 生成的工具调用会传入 timeout 参数（如 120000ms），但代码完全不读取这个参数。

## 根因分析

### 核心断点：containerPipInstallTimeoutSeconds 从未传递到 container_shell 执行路径

```
用户设置 pip install 超时 = 10s
    ↓ 保存到 Settings.containerPipInstallTimeoutSeconds

LLM 调用 container_shell 执行 "pip install numpy"
    ↓
LocalTools.getTools()
    → createContainerShellTool(sandboxId, enabledSkills, settings?.containerTimeoutSeconds ?: 300)
    ⚠️ 只传了 containerTimeoutSeconds（通用容器超时，300s）
    ❌ 没传 containerPipInstallTimeoutSeconds（pip install 专用超时，10s）
    ↓
createContainerShellTool.execute()
    → prootManager.executeShell(command, timeoutSeconds=300)
    ❌ pip install 10s 设置完全被绕过
    ↓
PRootManager.executeShell()
    → execInContainer(timeoutMs = 300 * 1000L = 300000ms)
    ❌ 实际执行超时是 300s，而不是用户设置的 10s
```

### 对比：pip list 超时为什么生效？

```
ContainerManagerSheet.kt
    → prootManager.getInstalledPackages(settings.containerPipListTimeoutSeconds)
    → PRootManager.getInstalledPackages(timeoutSeconds=10)
    → execInContainer(timeoutMs = 10 * 1000L)  ✅ 生效
```

pip list 有独立的函数且被直接调用时传入设置值。而 pip install 走的是通用 container_shell 路径，没有专用入口。

### executePython() 是死代码

PRootManager.executePython() 有 pipInstallTimeoutSeconds 参数，但全项目无任何调用者，pip install 实际走 container_shell → executeShell() 路径。

---

## 方案选型

### 方案一（已否决）：命令拆分分段执行

将混合命令按 &&/||/; 拆分，每段用对应超时值单独执行。

否决原因：
- 拆分引入安全漏洞（拆分后子命令绕过安全检查）
- 环境隔离问题（每段独立执行，环境变量/工作目录不共享）
- 拆分器复杂性（需处理引号、换行符、命令替换等）
- 安全审查发现多个可绕过场景

### 方案二（已采纳）：整体执行 + 动态超时选择

不拆分命令，整体执行。根据命令内容动态选择超时值：

| 命令类型 | 超时策略 | 示例 |
|----------|----------|------|
| 纯 pip install | pipInstallTimeout（用户设置值） | pip install numpy → 10s |
| 纯 pip list | pipListTimeout（用户设置值） | pip list → 30s |
| 不含 pip | containerTimeout（用户设置值） | ls -la → 300s |
| 含 pip 的混合命令 | max(相关pip超时, containerTimeout) | pip install numpy && ls -la → max(10, 300) = 300s |

优势：
- 不拆分 → 无拆分安全风险
- 整体执行 → 环境一致
- 混合命令 pip 部分的超时设置不完全生效（取 max），但至少整体不会超时
- 纯 pip 命令的超时设置完全生效 ✓

---

## 修复方案详情

### 新增函数：selectShellTimeout

根据命令内容选择超时值，纯函数无副作用：

1. 纯 pip install（命令以 pip/python 开头且不含其它命令组合符）→ pipInstallTimeoutSeconds
2. 纯 pip list/show/freeze/config → pipListTimeoutSeconds
3. 不含 pip 的命令 → defaultTimeoutSeconds（即 containerTimeoutSeconds）
4. 含 pip 的混合命令（命令含 pip 但不以 pip/python 开头）→ max(pip超时, defaultTimeoutSeconds)
5. 同时含 pip install 和 pip list → max(pipInstall, pipList, defaultTimeout)

### 修改的函数签名

createContainerShellTool：增加 pipInstallTimeoutSeconds 和 pipListTimeoutSeconds 参数
createSandboxShellReadonlyTool：增加 pipListTimeoutSeconds 参数

### 修改的调用点（4处）

| 位置 | 修改 |
|------|------|
| LocalTools.getTools() readonly阶段 | 传入 settings?.containerPipListTimeoutSeconds ?: 30 |
| LocalTools.getTools() execute阶段 | 传入 settings?.containerPipInstallTimeoutSeconds ?: 120 和 pipListTimeoutSeconds |
| SubAgentExecutor readonly调用 | 传入 settings.containerPipListTimeoutSeconds |
| SubAgentExecutor container调用 | 传入 settings.containerPipInstallTimeoutSeconds 和 pipListTimeoutSeconds |

### execute 闭包修改

container_shell 和 sandbox_shell_readonly 的 execute 闭包中：
- 调用 selectShellTimeout() 计算 effectiveTimeout
- 用 effectiveTimeout 传给 prootManager.executeShell() 代替原来固定的 timeoutSeconds

---

## 附带修复：checkContainerCommandSecurity 正则增强

原有危险命令检测只匹配空格包围的模式（如 " rm "），不匹配分隔符紧贴场景（如 ;rm、&&rm）。

修复后使用正则匹配：
- 匹配规则：危险命令名前面是非字母字符或字符串开头，后面是空格/结尾/非标识符字符
- 覆盖场景：rm、;rm、&&rm、||rm 等所有边界

---

## 变更文件清单

| 文件 | 改动 |
|------|------|
| LocalTools.kt | +46 行核心（函数签名、execute 闭包、selectShellTimeout、正则增强） |
| SubAgentExecutor.kt | +2 行（两个调用点传入超时参数） |

---

## 超时值传递链路核实

```
Settings（PreferencesStore）
  containerTimeoutSeconds          → 用户自定义（默认 300s，最低 10s）
  containerPipInstallTimeoutSeconds → 用户自定义（默认 120s，最低 10s）
  containerPipListTimeoutSeconds    → 用户自定义（默认 30s，最低 5s）
      ↓
getTools() / SubAgentExecutor
  传入 settings?.containerTimeoutSeconds ?: 300
  传入 settings?.containerPipInstallTimeoutSeconds ?: 120
  传入 settings?.containerPipListTimeoutSeconds ?: 30
      ↓
createContainerShellTool(... timeoutSeconds, pipInstallTimeoutSeconds, pipListTimeoutSeconds)
      ↓
selectShellTimeout(command, defaultTimeoutSeconds=timeoutSeconds, ...)
  defaultTimeoutSeconds = 用户自定义的容器命令超时 ✓
  pipInstallTimeoutSeconds = 用户自定义的 pip install 超时 ✓
  pipListTimeoutSeconds = 用户自定义的 pip list 超时 ✓
      ↓
prootManager.executeShell(sandboxId, command, effectiveTimeout)
```

所有超时值均来自用户设置，无硬编码。fallback 值（300/120/30）仅在 settings 为 null 时生效。

---

## 行为验收标准

### 纯 pip install
- 用户设置 pip install 超时 = 10s
- 执行 pip install numpy → 10s 后超时 ✓

### 纯 pip list
- 用户设置 pip list 超时 = 30s
- 执行 pip list → 30s 后超时 ✓

### 不含 pip 的普通命令
- 用户设置容器命令超时 = 300s
- 执行 ls -la → 300s 后超时 ✓

### 含 pip 的混合命令
- 用户设置 pip install = 10s，容器命令 = 300s
- 执行 pip install numpy && ls -la → max(10, 300) = 300s
- pip install 的 10s 设置不完全生效，但整体不会超时

### 安全检查（独立修复）
- 含 rm 等危险命令的组合 → 安全检查拦截 ✓
- 分隔符紧贴的危险命令（如 &&rm）→ 正则增强后拦截 ✓

---

## 风险提示

- 本容器无 Java/Kotlin 编译环境，无法执行 Gradle 编译验证。
- 建议在本地运行：
  - ./gradlew :app:compileDebugKotlin
  - 重点观察 LocalTools.kt 中新增的 selectShellTimeout 函数和修改的函数签名。
  - 检查 SubAgentExecutor.kt 中 createContainerShellTool 调用参数是否过长需要格式化。
